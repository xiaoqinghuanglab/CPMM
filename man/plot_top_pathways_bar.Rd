% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_top_pathways_bar.R
\name{plot_top_pathways_bar}
\alias{plot_top_pathways_bar}
\title{Top pathways by gene count (bar plot)}
\usage{
plot_top_pathways_bar(
  df,
  pathway_col = "Cleaned_Pathway",
  gene_col = "Gene",
  logq_col = "LogQValue",
  category_col = "BioCategory_Manual",
  top_n = 15,
  palette = NULL,
  annotate = TRUE,
  annotation_format = "LogQ = {val:.2f}",
  title = "Top Pathways by Gene Count",
  export = FALSE,
  export_dir = "Figures",
  export_name = "top_pathways_bar",
  export_formats = c("pdf", "svg")
)
}
\arguments{
\item{df}{Data frame with pathway enrichment results.}

\item{pathway_col}{Column for pathway names. Default \code{"Cleaned_Pathway"}.}

\item{gene_col}{Column for gene identifiers. Default \code{"Gene"}.}

\item{logq_col}{Column for log-transformed q-values (e.g., -log10 FDR). Default \code{"LogQValue"}.}

\item{category_col}{Column for biological categories. Default \code{"BioCategory_Manual"}.}

\item{top_n}{Number of pathways to display. Default \code{15}.}

\item{palette}{Optional vector of hex colors for categories. If \code{NULL}, a default
palette is used and truncated to the number of categories.}

\item{annotate}{Annotate bars? Default \code{TRUE}.}

\item{annotation_format}{Template for annotations. Supports \verb{\{val:.2f\}}, \verb{\{val:.1e\}}, or \code{{val}}.
Default \code{"LogQ = {val:.2f}"}.}

\item{title}{Plot title. Default \code{"Top Pathways by Gene Count"}.}

\item{export}{Save figure to disk? Default \code{FALSE}.}

\item{export_dir}{Output directory. Default \code{"Figures"}.}

\item{export_name}{Base filename (no extension). Default \code{"top_pathways_bar"}.}

\item{export_formats}{Vector of formats (e.g., \code{c("pdf","svg")}).}
}
\value{
A \code{ggplot} object.
}
\description{
Ranks pathways by unique gene count, colors by biological category, and
annotates each bar with a label derived from the max of \code{logq_col}
per pathway (default: "LogQ = {val:.2f}").
}
